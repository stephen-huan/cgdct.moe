#!/bin/bash
# script to validate html/css/svg with the Nu Html Checker (v.Nu)
# https://github.com/validator/validator

vnu="$(dirname "$0")/node_modules/vnu-jar/build/dist/vnu.jar"
java -jar "$vnu" \
    --asciiquotes \
    --also-check-css \
    --also-check-svg \
    --skip-non-html \
    "$@"

