#!/usr/bin/env bash
# script to serve the site locally
set -o errexit
set -o nounset
set -o pipefail

filter="bin/filter"

# https://unix.stackexchange.com/a/7080
julia --optimize=0 --color=yes --eval "using Franklin: serve
  serve(prerender=true)" 2> >(sh "$filter" >&2)
